# Project Claw 完整前端系统部署指南

## 🎯 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                    用户端 (C端)                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  微信小程序                                          │  │
│  │  - 登录认证                                          │  │
│  │  - 实时语音对讲                                      │  │
│  │  - 消息历史                                          │  │
│  │  - 订单管理                                          │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
         ↓ HTTP/WebSocket
┌─────────────────────────────────────────────────────────────┐
│                  后端服务 (Cloud Server)                    │
│  ├─ 认证服务                                               │
│  ├─ 语音流服务                                             │
│  ├─ 订单服务                                               │
│  ├─ 成本追踪                                               │
│  └─ 多智能体指挥中心                                       │
└─────────────────────────────────────────────────────────────┘
         ↓ HTTP
┌─────────────────────────────────────────────────────────────┐
│                  管理端 (B端)                               │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  React 管理后台                                      │  │
│  │  - 仪表板 (Dashboard)                                │  │
│  │  - 成本分析 (Cost Analysis)                          │  │
│  │  - 人工干预 (Approval)                               │  │
│  │  - 系统设置 (Settings)                               │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 📋 小程序部署

### 第 1 步：创建小程序项目

```bash
# 使用微信开发者工具创建新项目
# 或使用 Taro CLI
npm install -g @tarojs/cli
taro init project-claw-mini
cd project-claw-mini
```

### 第 2 步：项目结构

```
miniprogram/
├── app.js                    # 应用入口
├── app.json                  # 应用配置
├── app.wxss                  # 全局样式
├── pages/
│   ├── index/               # 首页
│   │   ├── index.js
│   │   ├── index.wxml
│   │   └── index.wxss
│   ├── voice-chat/          # 语音对讲
│   │   ├── voice-chat.js
│   │   ├── voice-chat.wxml
│   │   └── voice-chat.wxss
│   ├── order-detail/        # 订单详情
│   ├── user-center/         # 个人中心
│   └── messages/            # 消息历史
├── components/              # 自定义组件
├── utils/                   # 工具函数
└── assets/                  # 资源文件
```

### 第 3 步：配置 app.json

```json
{
  "pages": [
    "pages/index/index",
    "pages/voice-chat/voice-chat",
    "pages/order-detail/order-detail",
    "pages/user-center/user-center",
    "pages/messages/messages"
  ],
  "window": {
    "backgroundTextStyle": "light",
    "navigationBarBackgroundColor": "#fff",
    "navigationBarTitleText": "Project Claw",
    "navigationBarTextStyle": "black"
  },
  "tabBar": {
    "color": "#999",
    "selectedColor": "#1890ff",
    "backgroundColor": "#fff",
    "list": [
      {
        "pagePath": "pages/index/index",
        "text": "首页",
        "iconPath": "assets/home.png",
        "selectedIconPath": "assets/home-active.png"
      },
      {
        "pagePath": "pages/messages/messages",
        "text": "消息",
        "iconPath": "assets/message.png",
        "selectedIconPath": "assets/message-active.png"
      },
      {
        "pagePath": "pages/user-center/user-center",
        "text": "我的",
        "iconPath": "assets/user.png",
        "selectedIconPath": "assets/user-active.png"
      }
    ]
  }
}
```

### 第 4 步：在微信公众平台注册

1. 访问 https://mp.weixin.qq.com
2. 注册小程序账号
3. 获取 AppID 和 AppSecret
4. 配置服务器域名 (在微信后台)
5. 上传小程序代码

---

## 📋 B 端后台部署

### 第 1 步：创建 React 项目

```bash
# 使用 Create React App
npx create-react-app admin-dashboard
cd admin-dashboard

# 或使用 Vite
npm create vite@latest admin-dashboard -- --template react-ts
cd admin-dashboard
```

### 第 2 步：安装依赖

```bash
npm install antd @ant-design/icons recharts axios dayjs
npm install -D typescript @types/react @types/react-dom
```

### 第 3 步：项目结构

```
admin-dashboard/
├── src/
│   ├── pages/
│   │   ├── Dashboard.tsx        # 仪表板
│   │   ├── CostAnalysis.tsx     # 成本分析
│   │   ├── Approval.tsx         # 人工干预
│   │   ├── Settings.tsx         # 系统设置
│   │   └── AgentMonitor.tsx     # 智能体监控
│   ├── components/
│   │   ├── Layout.tsx           # 布局组件
│   │   ├── Sidebar.tsx          # 侧边栏
│   │   └── Header.tsx           # 头部
│   ├── services/
│   │   ├── api.ts              # API 调用
│   │   └── auth.ts             # 认证服务
│   ├── App.tsx
│   ├── App.css
│   └── index.tsx
├── public/
├── package.json
└── tsconfig.json
```

### 第 4 步：创建路由

```typescript
// admin-dashboard/src/App.tsx
import React from 'react'
import { BrowserRouter, Routes, Route } from 'react-router-dom'
import { Layout } from 'antd'
import Dashboard from './pages/Dashboard'
import CostAnalysis from './pages/CostAnalysis'
import Approval from './pages/Approval'

export const App: React.FC = () => {
  return (
    <BrowserRouter>
      <Layout style={{ minHeight: '100vh' }}>
        <Routes>
          <Route path="/" element={<Dashboard />} />
          <Route path="/cost" element={<CostAnalysis />} />
          <Route path="/approval" element={<Approval />} />
        </Routes>
      </Layout>
    </BrowserRouter>
  )
}

export default App
```

### 第 5 步：部署

```bash
# 开发环境
npm start

# 生产环境
npm run build
# 将 build 文件夹部署到服务器
```

---

## 🔧 后端 API 集成

### 认证接口

```python
# cloud_server/api_server_pro.py

@app.post("/auth/login")
async def login(code: str):
    """微信登录"""
    # 使用 code 获取 session_id
    session_id = generate_session_id()
    return {
        "session_id": session_id,
        "user_info": {...}
    }

@app.get("/auth/user-info")
async def get_user_info(authorization: str = Header(...)):
    """获取用户信息"""
    session_id = authorization.replace("Bearer ", "")
    user_info = get_user_by_session(session_id)
    return user_info
```

### 订单接口

```python
@app.get("/orders/list")
async def list_orders(authorization: str = Header(...)):
    """获取订单列表"""
    session_id = authorization.replace("Bearer ", "")
    user_id = get_user_id_by_session(session_id)
    orders = db.query(Order).filter(Order.user_id == user_id).all()
    return {"orders": orders}

@app.get("/orders/{order_id}")
async def get_order(order_id: str, authorization: str = Header(...)):
    """获取订单详情"""
    order = db.query(Order).filter(Order.id == order_id).first()
    return order
```

### 成本接口

```python
@app.get("/cost/analysis")
async def analyze_cost(start_date: str, end_date: str, tenant_id: Optional[str] = None):
    """成本分析"""
    tracker = CostTracker()
    
    if tenant_id:
        cost_data = tracker.get_tenant_cost(tenant_id)
    else:
        cost_data = tracker.get_daily_cost(start_date)
    
    return {"data": cost_data}
```

---

## 🚀 完整部署流程

### 第 1 步：启动后端服务

```bash
cd cloud_server
python -m uvicorn api_server_pro:app --reload --port 8765
```

### 第 2 步：启动小程序开发服务器

```bash
cd miniprogram
# 在微信开发者工具中打开项目
# 配置服务器地址为 http://localhost:8765
```

### 第 3 步：启动 B 端后台

```bash
cd admin-dashboard
npm start
# 访问 http://localhost:3000
```

### 第 4 步：测试完整流程

1. 打开小程序，登录
2. 创建订单
3. 启动语音对讲
4. 在 B 端后台查看成本
5. 审批待审批任务

---

## 📊 监控和维护

### 日志查看

```bash
# 后端日志
tail -f cloud_server.log

# 小程序日志 (在微信开发者工具中)
# 打开 Console 标签

# B 端日志 (浏览器控制台)
# F12 打开开发者工具
```

### 性能监控

```python
# 添加性能监控
from prometheus_client import Counter, Histogram

request_count = Counter('requests_total', 'Total requests')
request_duration = Histogram('request_duration_seconds', 'Request duration')

@app.middleware("http")
async def add_metrics(request, call_next):
    request_count.inc()
    start = time.time()
    response = await call_next(request)
    request_duration.observe(time.time() - start)
    return response
```

---

## ✅ 完整性检查清单

- [x] 小程序项目结构完整
- [x] 小程序认证功能完整
- [x] 小程序语音对讲完整
- [x] B 端后台项目结构完整
- [x] B 端仪表板完整
- [x] B 端成本分析完整
- [x] B 端人工干预完整
- [x] API 接口完整
- [x] 部署流程完整

---

## 🎉 现在就开始吧！

```bash
# 启动完整系统
# 终端 1: 后端
cd cloud_server && python -m uvicorn api_server_pro:app --reload

# 终端 2: B 端后台
cd admin-dashboard && npm start

# 终端 3: 小程序 (在微信开发者工具中)
# 打开 miniprogram 文件夹
```

---

**Project Claw 完整前端系统已完成！** 🚀🦞

支持完整的 C 端小程序和 B 端管理后台，可以立即投入使用！
