global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'project-claw-signaling'
    metrics_path: /metrics
    static_configs:
      - targets: ['signaling-server:8765']
