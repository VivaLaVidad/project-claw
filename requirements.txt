# requirements.txt - Project Claw v3.0 精简生产依赖

# ── Web 框架 ──
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
httpx>=0.27.0

# ── 数据验证 ──
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── LLM ──
requests>=2.32.0
tenacity>=8.2.0

# ── 安全通信 ──
cryptography>=42.0.0

# ── 状态存储 ──
redis>=5.0.0

# ── 订单存储 ──
aiosqlite>=0.20.0

# ── 配置 ──
python-dotenv>=1.0.0

# ── 日志 ──
rich>=13.7.0

# ── 监控大屏 ──
streamlit>=1.35.0
pandas>=2.0.0
pydeck>=0.9.0
websocket-client>=1.8.0

# ── 可选：Edge Box OCR（本地运行时取消注释）──
# uiautomator2>=2.16.11
# easyocr>=1.7.0
# opencv-python>=4.8.0
# numpy>=1.24.0
# torch>=2.0.0
# Pillow>=10.0.0

# ── 可选：RAG（本地运行时取消注释）──
# chromadb>=0.4.0
# langchain-core>=0.1.0
# langgraph>=0.1.0
