# requirements.txt - Railway 云端生产依赖（精简版，仅核心服务）

# ── Web 框架 ──
fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
httpx==0.27.0

# ── 数据验证 ──
pydantic==2.7.1
pydantic-settings==2.2.1

# ── HTTP 请求 / 重试 ──
requests==2.32.2
tenacity==8.3.0

# ── 安全通信 ──
cryptography==42.0.8

# ── 存储 ──
redis==5.0.4
aiosqlite==0.20.0

# ── 配置 ──
python-dotenv==1.0.1

# ── 日志 ──
rich==13.7.1
