# requirements.txt - Project Claw v14.3
# Railway 云端生产依赖（固定版本，确保可重现构建）

# ── Web 框架 ────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
httpx==0.27.0
python-multipart==0.0.9

# ── 数据验证 ────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1

# ── HTTP 请求 / 重试 ────────────────────────────────────────
requests==2.32.2
tenacity==8.3.0

# ── 安全通信 ────────────────────────────────────────────────
cryptography==42.0.8

# ── 存储 ────────────────────────────────────────────────────
redis==5.0.4
aiosqlite==0.20.0

# ── 配置 ────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── 日志 ────────────────────────────────────────────────────
rich==13.7.1

# ── 监控大屏（本地运行，非 Railway 核心依赖）────────────────
# streamlit==1.35.0
# pydeck==0.9.1
# pandas==2.2.2
# websocket-client==1.8.0

# ── 视觉识别（Edge Box 本地，非云端依赖）────────────────────
# easyocr==1.7.1
# Pillow==10.3.0
# numpy==1.26.4

# ── Android 驱动（Edge Box 本地）────────────────────────────
# uiautomator2==3.0.0

# ── LangGraph（可选，降级时跳过）────────────────────────────
# langgraph==0.1.9
